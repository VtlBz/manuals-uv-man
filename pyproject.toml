[project]
name = "uv-manual"
version = "0.1.0"
description = "Практическое руководство по менеджеру uv для Python-разработчиков"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = []
docs = [
    "mkdocs-material",
    "mkdocs-include-markdown-plugin",
]
